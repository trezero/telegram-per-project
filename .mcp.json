{
  "mcpServers": {
    "telegram": {
      "command": "bun",
      "args": ["server.ts"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {
        "NODE_PATH": "${CLAUDE_PLUGIN_DATA}/node_modules"
      }
    }
  }
}
